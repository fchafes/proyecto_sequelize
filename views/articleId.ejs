<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles.css" />
    <link rel="stylesheet" href="/articles.css" />
  </head>
  <body>
    <%- include("partials/navbar.ejs") %>
    <header>
      <div class="header-container">
        <div class="header-content">
          <h1><%= article.title %></h1>
          <p>
            Creado por <%= article.author.name %> <%= article.author.lastname %>
            el <%= article.customDate %>
          </p>
        </div>
      </div>
    </header>
    <main>
      <div class="container">
        <div class="row">
          <div class="col-md-6 offset-md-3 bg-white p-2">
            <p class="article-content"><%= article.content %></p>
            <hr />
            <h3 class="article-comment"><strong>Add your comment</strong></h3>

            <form action="/articleId/<%= article.id %>" method="POST">
              <input
                type="text"
                id="commentName"
                class="form-control"
                placeholder="Name"
                name="fullName"
              />
              <textarea
                class="form-control"
                id="textComment"
                rows="3"
                name="content"
              ></textarea>
              <button type="submit" class="btn btn-primary mb-3">Send</button>
            </form>
            <h2 class="comment-title">
              <% if (comments && comments.length > 0) { %>
              <strong>Comments </strong>(<span><%= comments.length %></span>) <%
              } else { %> No comments available. <% } %>
            </h2>

            <% for (const comment of comments) {%>
            <div class="comment">
              <blockquote class="comment-text">
                "<%= comment.content %>"
              </blockquote>
              <p class="comment-author"><%= comment.fullName %></p>
              <hr />
            </div>
            <% } %>
          </div>
        </div>
      </div>
      <%- include("partials/footer.ejs") %>
    </main>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
